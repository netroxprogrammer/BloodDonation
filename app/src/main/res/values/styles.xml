<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>
    <style name="DialogTheme" parent="@android:style/Theme.Dialog">
        <item name="android:alertDialogStyle">@style/CustomAlertDialogStyle</item>
        <item name="android:textColorPrimary">#000000</item>

    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="CustomAlertDialogStyle">
        <item name="android:bottomBright">@color/progressbaseColor</item>
        <item name="android:bottomDark">@color/progressbaseColor</item>
        <item name="android:bottomMedium">@color/progressbaseColor</item>
        <item name="android:centerBright">@color/progressbaseColor</item>
        <item name="android:centerDark">@color/progressbaseColor</item>
        <item name="android:centerMedium">@color/progressbaseColor</item>
        <item name="android:fullBright">@color/progressbaseColor</item>
        <item name="android:fullDark">@color/progressbaseColor</item>
        <item name="android:topBright">@color/progressbaseColor</item>
        <item name="android:topDark">@color/progressbaseColor</item>
    </style>
</resources>
